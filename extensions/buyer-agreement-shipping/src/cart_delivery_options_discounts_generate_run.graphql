query Input {
  cart {
    deliveryType: attribute(key: "Tipo de entrega") {
      value
    }
    deliveryGroups {
      deliveryOptions {
        handle
      }
    }
  }
  discount {
    discountClasses
  }
}